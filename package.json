{
  "name": "purescript-wags",
  "version": "1.1.6",
  "description": "Web Audio Graphs as a Stream",
  "scripts": {
    "build": "spago build",
    "format": "purs-tidy format-in-place src/**/*.purs",
    "pulp-publish": "pulp publish",
    "docs": "pulp docs",
    "test": "spago -x test.dhall test && spago -x examples.dhall build && spago -x cheatsheet.dhall build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.14.36",
    "psc-package": "^4.0.1",
    "pulp": "^15.0.0",
    "purs-tidy": "^0.9.0"
  },
  "dependencies": {
    "purescript": "^0.15.0",
    "spago": "^0.20.8"
  }
}
